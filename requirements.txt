# 物料汇算系统 - Python 依赖
# 安装: pip install -r requirements.txt
# 或使用一键脚本: setup.bat (Windows) / bash setup.sh (Linux)

# ---------- 必需依赖 ----------
# Excel 报表导出
openpyxl>=3.0.0

# Web 界面 (Flask)
flask>=2.0.0

# ---------- 可选依赖 ----------
# 批量导入 Excel/CSV 文件时需要
pandas>=1.3.0

# ---------- 测试/开发 ----------
# pytest>=7.0.0
